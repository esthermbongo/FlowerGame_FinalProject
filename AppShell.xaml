<?xml version="1.0" encoding="UTF-8" ?>
<Shell x:Class="FlowerGame.AppShell"
       xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:FlowerGame.Views">

    <ShellContent Title="Setup" Route="MainPage" ContentTemplate="{DataTemplate views:MainPage}" />
    <ShellContent Title="Game" Route="GamePage" ContentTemplate="{DataTemplate views:GamePage}" />

</Shell>